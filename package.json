{
    "name": "side-initiative",
    "private": true,
    "type": "module",
    "scripts": {
        "test": "node --import tsx --test tests/*.test.ts",
        "build": "tsc -p tsconfig.json",
        "build:watch": "tsc -p tsconfig.json --watch",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "createSymlinks": "tsx tools/create-symlinks.ts",
        "postinstall": "npm run createSymlinks",
        "changelog": "git-cliff -o CHANGELOG.md",
        "release:build": "tsx tools/build-release.ts"
    },
    "devDependencies": {
        "@types/node": "^26.0.0",
        "fvtt-types": "^13.346.0-beta.20250812191140",
        "js-yaml": "^4.2.0",
        "tsx": "^4.22.4",
        "typescript": "^5.9.3"
    }
}
